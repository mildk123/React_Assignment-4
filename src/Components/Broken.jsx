import React,{Component} from 'react';
import ShatteredState from '../images/broken.png'

class Broken extends Component{
    render(){
        return(
            <img src={ShatteredState} alt="ShatteredState"></img>
        )
    }
}

export default Broken;